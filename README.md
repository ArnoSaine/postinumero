# @postinumero

Miscellaneous utility packages 🛠

## Story

Long ago, before JavaScript bundler tools existed, there was a project. All the project's helper functions were put into a file named `postinumero.js` (`zipcode.js`), because “it was one of the first scripts loaded on the page”. This repository pays homage to that magnificent file. 😉
